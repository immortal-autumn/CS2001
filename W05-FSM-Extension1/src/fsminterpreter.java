import common.Setup;

public class fsminterpreter {
    /**
     * As I have programmed varieties of classes and saved them in different packages, I put main class in default package in order for starting the stacscheck.
     * @param args are arguments that users need to input,
     */
    public static void main(String[] args) {
        Setup.starter(args);
    }
}
